// Generated by CoffeeScript 1.6.2
(function() {
  $(document).ready(function() {
    var getSphere, init;

    init = function() {
      var $container, ASPECT, FAR, HEIGHT, NEAR, VIEW_ANGLE, WIDTH, camera, pointLight, renderLoop, renderer, scene, sphere;

      WIDTH = 800;
      HEIGHT = 600;
      VIEW_ANGLE = 45;
      ASPECT = WIDTH / HEIGHT;
      NEAR = 0.1;
      FAR = 10000;
      $container = $('#container');
      renderer = new THREE.WebGLRenderer();
      scene = new THREE.Scene();
      camera = new THREE.PerspectiveCamera(VIEW_ANGLE, ASPECT, NEAR, FAR);
      camera.position.z = 300;
      scene.add(camera);
      sphere = getSphere();
      scene.add(sphere);
      pointLight = new THREE.PointLight(0xFFFFFF);
      pointLight.position.x = 10;
      pointLight.position.y = 50;
      pointLight.position.z = 130;
      scene.add(pointLight);
      renderer.setSize(WIDTH, HEIGHT);
      $container.prepend(renderer.domElement);
      TweenMax.to(sphere.position, 3, {
        startAt: {
          z: -200
        },
        z: 200,
        repeat: -1,
        yoyo: true
      });
      renderLoop = function() {
        renderer.render(scene, camera);
        return requestAnimationFrame(renderLoop);
      };
      return requestAnimationFrame(renderLoop);
    };
    getSphere = function(scene) {
      var radius, rings, segments, sphere;

      radius = 50;
      segments = 16;
      rings = 16;
      return sphere = new THREE.Mesh(new THREE.SphereGeometry(radius, segments, rings), new THREE.MeshLambertMaterial({
        color: 0xCC0000
      }));
    };
    return init();
  });

}).call(this);
